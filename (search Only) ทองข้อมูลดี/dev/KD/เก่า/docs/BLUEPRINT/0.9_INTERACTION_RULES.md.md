จัดให้แบบ **ฉบับสมบูรณ์**, ครอบคลุมที่สุด, อ่านง่าย, และสอดคล้องกับทุก Blueprint ก่อนหน้า (0.1–0.8), SYSTEM_CONTRACT, AGENT_FLOW, PANEL_DEFINITIONS, DATA_MODEL และ PERMISSIONS

---

# 🟩 **BLUEPRINT 0.9 — INTERACTION_RULES (FULL VERSION)**

**กติกาการโต้ตอบทั้งหมดในแพลตฟอร์ม UET ระหว่าง:**

- ผู้ใช้ ↔ UI
    
- ผู้ใช้ ↔ ระบบ
    
- ผู้ใช้ ↔ AI
    
- Panel ↔ Panel
    
- Page ↔ Database
    
- AI ↔ Knowledge Base
    
- AI ↔ Sistema Permission
    

> ไฟล์นี้คือ “กฎจราจรของทั้งระบบ”  
> เพื่อไม่ให้ AI มั่ว, ผู้ใช้ไม่งง, และระบบไม่ conflict

---

# 🔰 0. PURPOSE

ทำให้ทุกการโต้ตอบใน UET predictable, consistent, safe, และ scalable

“กติกานี้คือภาษาเดียวที่ใช้ทั้ง Platform, Backend, AI Agents และ UX”

---

# 🟩 **1. INTERACTION LAYERS (4 ชั้นพื้นฐาน)**

```
L1: User → Platform UI  
L2: UI → System Logic (Panels / Pages / Actions)
L3: System Logic → Data Engine (Database + Vector + Cache)
L4: AI Layer → Knowledge Model + Permission Filter
```

ทุก interaction ต้องไหลผ่านเส้นทางนี้เสมอ

---

# 🟩 **2. USER INTERACTION PRINCIPLES (หลักการใหญ่)**

### **2.1 Predictability — ต้องเดาได้**

ผู้ใช้กดอะไร → ต้องเกิดผลลัพธ์ที่ “ตรงตามบริบท” เสมอ  
เช่น

- ถ้าอยู่ใน Project → Chat ให้ตอบด้วยฐานข้อมูลของ Project เท่านั้น
    
- ถ้าอยู่ใน Theory → Chat ให้ตอบด้วย Global KB
    

### **2.2 Context Isolation — บริบทต้องแยกเป็นส่วน ๆ**

AI ห้ามผสมข้อมูลระหว่าง:

- Private Chat
    
- Friend Chat
    
- Project
    
- Global KB
    

### **2.3 Minimal Surprise — ห้ามทำอะไรแปลกโดยไม่ได้สั่ง**

ตัวอย่างที่ **ห้าม**:

- แก้ไฟล์เอง
    
- สร้างไฟล์โดยไม่แจ้ง
    
- ย้ายข้อมูล
    
- Query KPI โดยผู้ใช้ไม่ได้ขอ
    

### **2.4 Consistency — ไม่ว่าหน้าไหนก็ใช้กฎเดียวกัน**

ทุก Panel ใช้ pattern interaction เดียว:

```
User → Intent → System Action → AI/Data → Result → Log
```

---

# 🟩 **3. PANEL INTERACTION RULES**

(กฎเฉพาะของแต่ละ Panel)

---

## **3.1 CHAT PANEL — กฎโต้ตอบกับ AI**

### **3.1.1 Chat มี 3 โหมดหลัก**

|Mode|ใช้เมื่อ|Source|
|---|---|---|
|Private|คุยกับ AI ส่วนตัว|personal KB|
|Friend|คุยกับเพื่อนหรือ AI ตัวกลาง|shared KB|
|Project / Community|คุยเรื่องงานหรือกลุ่ม|project KB|

### **3.1.2 Interaction Rules**

✔ AI ต้องตอบเฉพาะข้อมูลที่ role นั้นเข้าถึงได้  
✔ AI ต้องใช้ _Knowledge Model ตาม panel ที่อยู่_  
✔ AI ห้าม “link ข้าม context” เอง  
✔ ถ้าคำสั่งไม่ชัด → AI ต้องถามต่อ  
✔ ถ้าผู้ใช้ขอทำสิ่งที่เกินสิทธิ์ → ต้อง fallback

ตัวอย่าง Fallback Correct:

> “คุณไม่มีสิทธิ์เข้าถึง KPI ของโปรเจกต์นี้ แต่ฉันสามารถสร้าง Template วิเคราะห์ให้ได้นะครับ”

### **3.1.3 Autoprompt Interaction**

เมื่อกดฟังก์ชัน Autoprompt:

1. ตรวจ permission
    
2. ตรวจว่าต้องใช้ data อย่างไร
    
3. Run agent
    
4. Produce output เป็นไฟล์ใหม่เสมอ
    
5. ไม่ทับไฟล์เก่า
    

---

## **3.2 SOURCE PANEL — Interaction Rules**

Source = ศูนย์รวม **ไฟล์, note, KB, graph**

### กฎ:

✔ แสดงเฉพาะไฟล์ที่ user มีสิทธิ์  
✔ เปิดไฟล์ → ส่งไป Studio  
✔ ลิงก์ไฟล์ใน graph → navigate โดยไม่ reload  
✔ Upload ไฟล์ใหม่ → trigger ingest → สร้าง vector

**AI interaction**

- AI use source only if user is inside a page linked to the file
    
- AI cannot read disallowed files even if referenced by another file
    

---

## **3.3 STUDIO PANEL — Interaction Rules**

Studio = ที่สำหรับ _เขียน note, markdown, research, document_

### กฎ:

✔ ทุกการสร้าง note ใหม่ = ไฟล์ใหม่ (ไม่ overwrite)  
✔ การแก้ note ต้องทำผ่าน permission filter  
✔ Autoprompt ให้ผลลัพธ์เป็นไฟล์เสมอ  
✔ Studio preview → เปลี่ยน mode: Edit / Preview / AI Suggest

**AI interaction**

- AI ทำงานได้เฉพาะในพื้นที่ที่ role เขียนได้
    
- AI ห้ามแก้ note โดยตรง ต้องสร้าง “Patch Suggestion”
    
- AI สามารถสรุป note, link note, map concept
    

---

# 🟩 **4. PAGE INTERACTION RULES**

ทุก Page ทำงานด้วย pattern เดียว:

```
Page Loaded  
→ Load Panel States  
→ Load Page Data (based on role & ID)  
→ Attach AI Context  
→ Render  
```

---

# 🟩 **5. SYSTEM INTERACTION RULES**

กฎที่อยู่เบื้องหลังทุก interaction บนระบบ

---

## **5.1 Permission First**

ก่อนที่ระบบจะทำ _ทุก action_ ต้องเช็ก permission:

```
Action Request  
→ Identify User Role  
→ Check Permission Matrix  
→ Allow / Deny
```

---

## **5.2 Context Routing**

AI ต้อง route ตาม context:

|Location|AI Context Source|
|---|---|
|/chat/private|personal KB|
|/chat/friend|shared KB|
|/project/*|project KB|
|/theory/*|global KB|
|/studio|active file|

---

## **5.3 DB Interaction Rules**

✔ อ่านข้อมูลได้เฉพาะสิทธิ์  
✔ เขียนข้อมูลต้องผ่านระบบ patch  
✔ ลบข้อมูลต้องเป็น soft-delete ก่อนเสมอ  
✔ Action ทั้งหมดต้องถูก log

---

## **5.4 Knowledge Sync Rules**

เพื่อป้องกัน _ข้อมูลมั่ว / KB สกปรก_

✔ ข้อมูลใน Project KB  
→ ใช้ใน Project เท่านั้น  
✔ ถ้าจะ sync ไป Global KB  
→ ต้องผ่านคน + AI review  
✔ AI ไม่ sync อัตโนมัติเด็ดขาด

---

## **5.5 Event Bus Interaction**

ทุก action ส่งออกเป็น event เช่น:

```
FILE.CREATED
FILE.UPDATED
PROJECT.MEMBER_ADDED
AUTOPROMPT.EXECUTED
AI.RESULT.GENERATED
```

เพื่อให้ระบบอื่น react ได้ เช่น:

- update graph
    
- rebuild vector
    
- update dashboard
    

---

# 🟩 **6. AI INTERACTION RULES (ระดับลึก)**

### **6.1 AI ห้ามใช้ข้อมูลเกินสิทธิ์**

ข้อมูลที่ถูก Permission Deny = AI ต้องไม่รู้ว่ามีอยู่

### **6.2 AI Behavior ถูกกำหนดโดย Blueprint 0.7**

AI ต้องทำตาม:

- System Contract
    
- Safety Layer
    
- User Role
    
- Interaction Rules
    

### **6.3 AI Explainability**

AI ต้องระบุ:

- ใช้ข้อมูลจากไหน
    
- วิเคราะห์ยังไง
    
- ไม่คาดเดาสิ่งที่ไม่มีข้อมูล
    

---

# 🟩 **7. ERROR INTERACTION RULES**

เมื่อเกิดข้อผิดพลาด:

1. AI ต้องบอกสาเหตุแบบเข้าใจง่าย
    
2. เสนอวิธีแก้
    
3. ห้าม blame user
    
4. Log ลงระบบ
    

---

# 🟩 **8. INTERACTION DIAGRAM (HIGH-LEVEL)**

```
USER  
  │  
  ▼  
UI ACTION  
  │  
  ▼  
SYSTEM LOGIC  
  │  (Permission Check)  
  ▼  
DATA ENGINE  
  │  (Query / Vector / Cache)  
  ▼  
AI LAYER  
  │  
  ▼  
RESULT → UI  
```

---

# 🟩 **9. INTERACTION MATRIX (แบบเข้าใจง่าย)**

|Interaction|User|UI|System|DB|AI|
|---|---|---|---|---|---|
|Open File|click|load panel|check permission|read file|apply context|
|Ask AI|type|send query|detect context|fetch vectors|answer|
|Save Note|save|validate|write|update file|sync KB|
|Run Autoprompt|click|show loading|execute agent|store result|generate output|
|View KPI|open|render|check permission|query stats|optional summary|

---

# 🟩 **10. SUMMARY – กฎสั้นที่สุด**

```
UET Interaction Rules =
1) Permission ก่อน
2) Context ชัดเจน
3) แยกข้อมูลแต่ละพื้นที่
4) AI ห้ามข้าม context
5) ทุก action เป็น event
6) ทุกอย่างต้อง predictable
```

---

# ถ้าต้องการต่อไปทำ:

✔ Blueprint 1.0 (Backend Contract)  
✔ Blueprint 1.1 (Front-end Component Map)  
✔ Blueprint 1.2 (Full Event Tree)  
✔ Next.js Implementation Plan  
✔ AI System Prompt (production-level)

บอกได้เลย เริ่มต่อให้ทันที!