# คู่มือการใช้ Git ฉบับ Solo Developer (ทำงานคนเดียว) 🚀

Git คือ **"จุด Save Game"** (Checkpoint) ของโค้ดเรา ช่วยให้เราทดลองแก้โค้ดได้เต็มที่ พังก็แค่ย้อนกลับ!

## 1. Flow การทำงานพื้นฐาน (Loop ที่ต้องจำ) 🔄

การเซฟงานใน Git (Commit) มี 3 ขั้นตอน:

1.  **Work**: แก้โค้ดตามปกติ
2.  **Stage (Add)**: เลือกไฟล์ที่จะเซฟ (หยิบใส่กล่อง)
3.  **Commit**: ปิดกล่องและแปะป้ายชื่อ (บันทึกลง History)

---

## 2. วิธีใช้งานใน VS Code + Git Graph 🖥️

### A. การ Commit (เซฟงาน)
1.  ไปที่แถบ **Source Control** (ไอคอนกิ่งไม้ด้านซ้าย หรือกด `Ctrl+Shift+G`).
2.  คุณจะเห็นไฟล์ที่มีการเปลี่ยนแปลงใน list.
3.  กดเครื่องหมาย **`+`** (Stage Changes) ที่ไฟล์ หรือกด `+` ตรงคำว่า *Changes* เพื่อเลือกทั้งหมด (Stage All).
4.  พิมพ์ข้อความในช่อง **Message** ด้านบน เช่น "แก้สีปุ่ม", "เพิ่มฟังก์ชันคำนวณ", "แก้บั๊ก A".
5.  กดปุ่ม **Commit** (สีน้ำเงิน). **เสร็จ!** 🎉

### B. การดู History (ย้อนอดีต)
1.  กดที่แถบ **Git Graph** ด้านล่าง (status bar) หรือเปิดจาก Command Palette.
2.  คุณจะเห็นจุดไข่ปลาเรียงกันเป็นเส้นทางรถไฟ 🚂
3.  **คลิกที่จุด** เพื่อดูว่า Commit นั้นเราแก้ไฟล์อะไรไปบ้าง.
4.  **คลิกขวาที่จุดเก่าๆ** -> เลือก **Checkout** เพื่อย้อนเวลากลับไปดูสภาพโค้ดตอนนั้นได้ (ถ้าดูเสร็จแล้วอยากกลับมาปัจจุบัน ให้ Checkout กลับมาที่ `master` หรือ `main`).

---

## 3. กฎเหล็กที่ทำให้ชีวิตง่ายขึ้น ✨

1.  **Commit บ่อยๆ**: อย่ารอแก้เสร็จทั้งโปรเจกต์ค่อย Commit. แก้ฟีเจอร์นึงเสร็จ -> Commit. แก้บั๊กเสร็จ -> Commit. (เหมือนเล่นเกม ผ่านด่านย่อยก็ Save ทีหนึ่ง).
2.  **Message สื่อสารรู้เรื่อง**: พิมพ์ให้ตัวเองในอนาคตอ่านรู้เรื่อง เช่น "Fit bug login" ดีกว่า "Fix" เฉยๆ.
3.  **อย่า Commit ไฟล์ขยะ**: ไฟล์จำพวก `runs/` (ผลรัน), `__pycache__`, หรือไฟล์ข้อมูลขนาดใหญ่ **ไม่ต้อง Commit** (เราตั้งค่า `.gitignore` ไว้กันพวกนี้แล้ว).

## 4. ถ้า Commit ผิดทำไง? 😱
ถ้าเพิ่ง Commit ไปแล้วลืมแก้บางอย่าง หรือพิมพ์ผิด:
1.  แก้ไฟล์ให้ถูก แล้ว Stage (`+`) ใหม่.
2.  ตอนจะ Commit ให้ติ๊กถูกตรงคำว่า **"Amend"** (ในเมนู Commit dropdown ของ VS Code).
3.  มันจะแก้ทับ Commit ล่าสุดให้เลย ไม่สร้างจุดใหม่ให้ลก.
