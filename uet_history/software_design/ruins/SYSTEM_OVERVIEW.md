# 🧱 SYSTEM_OVERVIEW — UET Platform (Big Picture)

> TL;DR — UET Platform คือ **AI-first knowledge workspace**  
> ที่ใช้โครงสร้าง 3 แผง (Source / Chat / Studio) + Multi-layer KB (Local & Global)  
> เพื่อให้ “คุยกับทฤษฎี / ไฟล์งาน / โปรเจกต์” ได้เหมือนคุยกับคน

---

## 1. ภาพรวมระบบ (High-level System View)

UET Platform = แพลตฟอร์มสำหรับ
- จัดการความรู้ / ทฤษฎี / โปรเจกต์
- ใช้ AI อ่านไฟล์ วิเคราะห์ และสร้างเนื้อหาใหม่
- เชื่อมต่อเศรษฐกิจในอนาคต (Wallet, KPI, Token) ผ่าน Service ฝั่งหลังบ้าน

โครงสร้างหลักของระบบแบ่งเป็น 3 ชั้นที่มนุษย์มองเห็น:

1. **Workspace UI (3 Panels)** — ตัวจอที่ใช้งานจริง
2. **Service Layer** — Project, Knowledge, Finance, Auth, AI Engine
3. **Knowledge Layer** — Local KB ต่อโปรเจกต์ + Global KB กลางของทั้งแพลตฟอร์ม

```text
User (Browser)
   │
   ▼
[ UET Workspace (Next.js UI — 3 Panels) ]
   │
   ▼
[ API / Services (Project, Knowledge, Finance, Auth, AI) ]
   │
   ▼
[ Data & Knowledge (PostgreSQL + pgvector, S3, Local KB, Global KB) ]
```

---
## 2. แกนกลางของ UX — 3 Panel Workspace

ทุกหน้า “ทำงานจริง” (เช่น Project Workspace, Theory Reader) จะอิง layout 3 panel เป็นหลัก

```
UET Workspace (3-panel Architecture)

┌──────────────────────────────────────────────┐
│                   WORKSPACE                  │
├───────────────┬───────────────┬──────────────┤
│ SOURCE        │ CHAT          │ STUDIO       │
└───────────────┴───────────────┴──────────────┘
```

|Panel|หน้าที่หลัก|Key Functions (ตัวอย่าง)|
|---|---|---|
|SOURCE|ที่รวม “ข้อมูลดิบ” ของโปรเจกต์|File list, Graph view, Metadata, Source search|
|CHAT|ช่องคุยกับ AI (หัวใจของระบบ)|ใช้ Local KB ก่อน → ถ้าไม่พอจึงเรียก Global KB, context-aware chat|
|STUDIO|พื้นที่เขียนงาน / โน้ต / Markdown|Markdown editor, AI assist, สร้างไฟล์ใหม่, Draft → Publish|
> ในเชิง UX: “Source = ที่เก็บของ”, “Chat = สมอง”, “Studio = มือที่ลงมือทำ”

---

## 3. Knowledge Architecture — Local KB vs Global KB

ระบบความรู้ของ UET ถูกออกแบบแบบ **Multi-layer KB** เพื่อให้ทั้ง “ลึกในโปรเจกต์” และ “กว้างระดับแพลตฟอร์ม” ได้พร้อมกัน

- **Local KB (ต่อ Project)**
    
    - เก็บ note, ไฟล์, graph, snapshot, embedding ของโปรเจกต์นั้น ๆ
        
    - ใช้เป็นแหล่งข้อมูลหลักเวลาคุยในหน้า Workspace
        
- **Global KB (ของทั้งแพลตฟอร์ม)**
    
    - เก็บ “ทฤษฎีหลัก / concept กลาง / knowledge ระดับโลก” (เช่น UET Core, global concepts)
        
    - ใช้เป็น fallback ถ้า Local KB ตอบไม่พอ
        

**RAG Flow แบบย่อ**

```
User Question
   ↓
Chat Panel
   ↓
AI Engine
   ↓
1) หาใน Local KB ของโปรเจกต์ก่อน
   ↓ ถ้าไม่เจอ/ไม่พอ
2) ขยายไป Global KB
   ↓
Compose คำตอบ + อ้างอิงกลับไปยังไฟล์/โน้ต
```

---
## 4. Main User Flow (มองจากมุมคนใช้)

ฟลोพื้นฐานที่แทบจะทุกหน้าจะเกี่ยวข้อง มีโครงแบบเดียวกันกับที่อธิบายในไฟล์โครงสร้างไฟล์โปรเจกต์

1. **เข้า Project / Theory Workspace**
2. **เลือกไฟล์จาก Source Panel**
    - ดู list, filter, search, หรือเปิด graph ดูความสัมพันธ์ก่อน
        
3. **อ่าน + แก้ไขใน Studio Panel**
    - แก้ Markdown, สร้าง note ใหม่, แตกไฟล์ย่อย
        
4. **ถาม-ตอบใน Chat Panel**
    - ให้ AI ช่วยสรุป, วิเคราะห์, เชื่อมโยง, เขียนต่อ, ตรวจ logic
        
5. **Sync / Commit**
    - บันทึกกลับไปยัง Local KB, อัปเดต graph + embeddings
        
6. (อนาคต) **เชื่อมกับ Wallet/KPI**
    - track impact ของโปรเจกต์, token/economy, KPI ของงาน
        

ฟลอนี้คือ “หน่วยพื้นฐาน” ที่สามารถเอาไปขยายต่อเป็น flow ย่อยอื่น ๆ  
เช่น Community feed, Theory reading mode, Financial dashboard ฯลฯ

---
## 5. Core Concept ของแพลตฟอร์ม

สรุปแก่นของ UET Platform ใน 4 จุด:

1. **Everything is a Project**
    - ทุกอย่างเริ่มที่ Project (หรือ Theory set) → มี Local KB ของตัวเอง
        
2. **3-panel Thinking**
    - มองงานทุกอย่างผ่านมุมมอง Source / Chat / Studio
    - ทำให้ออกแบบหน้าใหม่ง่าย เพราะยึด pattern เดียวกันทั้งแพลตฟอร์ม
        
3. **AI + Knowledge as One System**
    - AI ไม่ได้แค่ “ตอบแชท” แต่ต่อกับโครงสร้างไฟล์, graph, KB, KPI แบบจริงจัง
        
4. **Scalable Architecture**
    - รองรับ Service แยก (Project, Knowledge, Finance, Auth, AI)
    - ต่อขยายเป็น ecosystem ระดับโลกได้ (หลายประเทศ, หลายภาษา, หลายองค์กร)
        

---
## 6. ไฟล์/เอกสารที่ต่อจาก SYSTEM_OVERVIEW

ถ้าอยาก “ลงลึก” ต่อ ให้ไปอ่านไฟล์เหล่านี้:

- `ARCHITECTURE_MAP.md` — ภาคสถาปัตยกรรมเต็ม, service, infra, diagram ละเอียด
- `0.1_SYSTEM_CORE.md` — กติกาหลักของระบบ, core rules
- `0.2_LAYOUT_SYSTEM.md` — รายละเอียด layout + mapping หน้าไหนใช้ layout อะไร
- `0.3_PAGE_DEFINITIONS.md` — รายชื่อหน้า + panel/drawer/modal ที่เกี่ยวข้อง
- `0.4_PANEL_DEFINITIONS.md` — เจาะทีละ panel ว่าทำอะไรได้บ้าง

> ใช้งานจริง: ให้คนใหม่ + AI อ่าน `README.md` → `SYSTEM_OVERVIEW.md` → ค่อยลงไฟล์ BLUEPRINT อื่น ๆ

---
# 🟩 **1) SYSTEM OVERVIEW – Simple Diagram (ภาพรวมทั้งระบบ)**

```
                   ┌─────────────────────────┐
                   │        USER UI          │
                   │    (Next.js Workspace)  │
                   └───────────┬─────────────┘
                               │
                               ▼
          ┌────────────────────────────────────────────┐
          │              3-PANEL WORKSPACE              │
          │---------------------------------------------│
          │  SOURCE      │      CHAT       │   STUDIO   │
          │ (ข้อมูลดิบ)  │ (คุยกับ AI)    │ (เขียนงาน) │
          └──────┬─────────────┬──────────────┬────────┘
                 │             │              │
                 ▼             ▼              ▼
       ┌────────────────────────────────────────────────┐
       │                UET SERVICES LAYER              │
       │  Project • Knowledge • Finance • Auth • AI     │
       └───────────────┬───────────────┬───────────────┘
                       │               │
                       ▼               ▼
       ┌──────────────────────┐  ┌─────────────────────┐
       │   LOCAL KB per       │  │    GLOBAL KB        │
       │     Project          │  │ (ทฤษฎี/ความรู้รวม)      │
       └──────────────────────┘  └─────────────────────┘
```

**ดูภาพรวม:**

- UI = ตัวที่เราใช้งานจริง
- Workspace = 3 Panel (หัวใจของ UX)
- Services = ระบบหลังบ้านทั้งหมด
- Knowledge Layer = สมองสองชั้น (Local / Global)

---

# 🟦 **2) SYSTEM MATRIX — แบบอ่านง่ายที่สุด**

### 🧱 **A. Architecture Matrix (ระบบทั้งหมดในตารางเดียว)**

|ชั้นระบบ|มีอะไรบ้าง|หน้าที่|
|---|---|---|
|**UI Layer**|Next.js, 3-Panel Workspace|สิ่งที่ผู้ใช้เห็นและคลิก|
|**Workspace Layer**|Source / Chat / Studio|ที่ทำงานหลักของผู้ใช้|
|**Service Layer**|Project, Knowledge, Finance, Auth, AI Engine|ประมวลผล, คำนวณ, เก็บข้อมูล|
|**Knowledge Layer**|Local KB, Global KB, pgvector, embeddings|สมองของระบบ (ข้อมูลทั้งหมด)|
|**Storage Layer**|PostgreSQL, Object Storage|ที่เก็บไฟล์/โน้ต/metadata|

---

### 🟩 **B. 3-Panel Matrix (ทำความเข้าใจ Panel ใน 10 วินาที)**

|Panel|คืออะไร|ใช้ทำอะไร|
|---|---|---|
|**SOURCE**|ลิ้นชักข้อมูล|ดูไฟล์ / โน้ต / กราฟ / metadata|
|**CHAT**|สมอง AI|สั่งงาน / ถาม / วิเคราะห์ / research|
|**STUDIO**|โต๊ะทำงาน|เขียน note / Markdown / ทำงานจริง|

> **คิดง่าย ๆ:** Source = ที่เก็บของ, Chat = สมอง, Studio = มือของเรา

---

### 🟦 **C. Knowledge Flow Matrix (AI ทำงานยังไง)**

|ขั้นตอน|อะไรเกิดขึ้น|ไปต่อที่ไหน|
|---|---|---|
|1. ผู้ใช้ถาม|ส่งคำถามเข้า Chat panel|AI Engine|
|2. AI หาใน Local KB|อ่านไฟล์/โน้ตของโปรเจกต์|ถ้าไม่พอ → step 3|
|3. AI หาใน Global KB|ใช้ทฤษฎี/ความรู้รวม|ส่งกลับ|
|4. ผสมคำตอบ|เชื่อมข้อมูลจากทั้งสอง KB|Return|
|5. แสดงผล|กลับมาที่ Chat panel|พร้อมลิงก์/ไฟล์อ้างอิง|

---

### 🟧 **D. Project Lifecycle Matrix**

|ขั้นตอน|ทำอะไรในระบบ|เกี่ยวข้องกับอะไร|
|---|---|---|
|**เริ่มโปรเจกต์**|สร้างพื้นที่ + Local KB|Project Service|
|**อัปโหลดไฟล์**|Source panel|Knowledge Service|
|**วิเคราะห์/ถาม AI**|Chat panel|AI Engine + KB|
|**เขียนงาน**|Studio panel|File system + Metadata|
|**บันทึก/Commit**|sync → Local KB|Graph + Embeddings|
|**Publish to Global**|ดันขึ้น Global KB|Knowledge Global|

---

# 🟪 **3) SUPER SHORT VERSION (แปะหัวไฟล์ได้เลย)**

```
UET = 3 Panel Workspace (Source / Chat / Studio)
   + Multi-layer Knowledge (Local KB + Global KB)
   + Services (Project / Knowledge / Finance / Auth / AI)

Source = ข้อมูลทั้งหมดของโปรเจกต์  
Chat = สมองของระบบ  
Studio = พื้นที่เขียนงานจริง  

AI Flow: Local KB → ถ้าไม่พอต่อ Global KB → รวมคำตอบ  
```

---

ถ้าอยากให้ผมทำ

- **เวอร์ชันสี (Color-coded) แบบขั้นเทพ**,
- **เวอร์ชันแนวดำเขียวตรงกับธีม UET**,
- หรือ **เวอร์ชันสำหรับใส่ใน ARCHITECTURE_MAP.md โดยตรง**,

บอกได้เลย เดี๋ยวทำให้แบบสวยๆ พร้อมไปใช้งานจริงได้ทันที 🔥