[package]
name = "uet_core"
version = "0.1.0"
edition = "2021"

[dependencies]
ndarray = "0.15"
serde = { version = "1.0", features = ["derive"] }
thiserror = "1.0"
schemars = "0.8" # For JSON Schema generation (MCP compatible)
